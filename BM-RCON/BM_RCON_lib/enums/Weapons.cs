namespace BM_RCON.BM_RCON_lib
{
    enum WeaponID
    {
        // NB: the command "forceweap" takes name of weapons (string) and not weapons ID (int)
        // to update when weapons will be there
    }
}